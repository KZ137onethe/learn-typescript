{
  "extends": "../part/base.json",
  "compilerOptions": {
    // * 编译目标配置
    // 编译语言的版本
    "target": "ESNext",
    // 生成代码的模板标准
    "module": "commonjs",
    // TS 需要引入的库，即声明文件，es5默认引用了dom、es5、scripthost,如需要使用es的高级版本特性，通常需要配置，如es8的数组新特性需要引入"ES2019.Array"
    "lib": [
      "ESNext"
    ],

    // * 输出控制
    // 删除注释
    "removeComments": true,
    // 不输出文件，即编译后不会产生任何的JavaScript文件
    "noEmit": false,
    // 生成目标文件的sourceMap文件
    "sourceMap": true,

    // * 类型声明文件
    // 声明文件目录，默认时node_modules/@types
    "typeRoots": ["node_modules/@types", "types"],
    // 从 typeRoots 中选择要包含的特定类型包，其他包将被忽略。若设为空数组 []，则 禁用自动加载
    // "types": ["@learn-typescript/types"]
  }
}